function dados (){
   
    const alturaTxt = 0
    const pesoTxt = 0 

    let altura = Number(alturaTxt)((' Digite sua altura: '));
    let peso = Number(pesoTxt)(('Digite seu peso: '));

    
    soma = (altura * altura) / peso
    
    return altura;
    return peso;
    return soma;
    
}

module.exports = dados